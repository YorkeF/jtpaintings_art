Options -Indexes

RewriteEngine On

# Allow direct access to existing files (uploads, dist assets)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Route everything else through index.php
RewriteRule ^ index.php [L]
